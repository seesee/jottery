{
  "name": "jottery-webview-bundles",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained HTML bundles for iOS WKWebView editors",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && BUNDLE=preview vite build && BUNDLE=calc vite build && BUNDLE=outliner vite build",
    "build:preview": "BUNDLE=preview vite build",
    "build:calc": "BUNDLE=calc vite build",
    "build:outliner": "BUNDLE=outliner vite build",
    "dev": "vite"
  },
  "dependencies": {
    "@codemirror/commands": "^6.10.2",
    "@codemirror/language": "^6.10.8",
    "@codemirror/state": "^6.5.2",
    "@codemirror/view": "^6.36.5",
    "@lezer/highlight": "^1.2.1",
    "highlight.js": "^11.11.1",
    "marked": "^15.0.7",
    "mathjs": "^14.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
